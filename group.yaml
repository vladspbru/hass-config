#group: --------------------------------------------

Devices:
  view: yes
  icon: mdi:chip
  entities:
    - group.sonoff_mainswitch
    - group.sonoff_water
    - group.sonoff_sauna
    - group.sonoff_pow
    - group.sonoff_4pro
    - group.sonoff_bridge
    - group.tasmota_floodlight
    - group.sonoff_sonar
    - group.motion_detection
    - group.sonoff_workplace
    
Presence:
  view: yes
  icon: mdi:switch
  entities:
    - group.net_nmap
    - group.family
    - group.motion_detection
    
Weather:
  view: yes
  icon: mdi:weather-partlycloudy
  entities:
    - group.weather_yr
    - group.today
    - group.weather_map
    - camera.wu_doppler_weather

System:
  view: yes
  icon: mdi:clippy
  entities:
    - group.hass
    - group.sonoff_workplace
    - group.all_automations
    - group.all_scripts
    - group.debug
#--------------------------------------------

family:
  entities:
    - device_tracker.vlad_nexus

today:
  entities:
    - sun.sun
    - sensor.temp_out
    - sensor.temp_in
    
net_nmap:
  entities:
    - device_tracker.sonoff_95b510
    - device_tracker.sonoff_26575d
    - device_tracker.sonoff_f4bc40
    - device_tracker.sonoff_801E75
    - device_tracker.sonoff_B2DF94
    - device_tracker.sonoff_68B5C7
    - device_tracker.sonoff_68AC8D
    

sonoff_workplace:
  entities:
    - script.sonoff_qry_states
    - binary_sensor.sonoff_95b510_lwt
    - binary_sensor.sonoff_26575d_lwt
    - binary_sensor.sonoff_f4bc40_lwt
    - binary_sensor.sonoff_801e75_lwt
    - binary_sensor.sonoff_B2DF94_lwt
    - binary_sensor.sonoff_68B5C7_lwt
    - binary_sensor.sonoff_68AC8D_lwt
    - binary_sensor.tasmota_AB76C3_lwt
    


#debug:
#  entities:
#    - input_select.log_level

#tasmota_utils:
  #entities:
    #- script.sonoff_qry_states
    
